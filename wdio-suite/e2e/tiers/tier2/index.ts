import type { LocatorEntry } from '../../helpers/locatorStore.js';

/** Tier 2 — LLM text resolution (Claude API). Not yet implemented. */
export async function resolve(_entry: LocatorEntry) {
    return undefined;
}
